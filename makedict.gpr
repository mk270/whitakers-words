project Makedict is
    for Exec_Dir use "bin";
    for Source_Dirs use ("src");
    for Object_Dir use "obj";
    for Main use ("makedict.adb");

        package Compiler is
        for Default_Switches ("Ada") use ("-gnatwa");
        end Compiler;
end Makedict;

